{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "composite": true
  },
  "tsBuildInfoFile": "node_modules/.cache/.tsbuildinfo",
  // Prevent auto-import from build
  "exclude": ["./build"],
  "include": ["./src", "./tests"],
  "references": [
    {
      "path": "../types"
    }
  ]
}
