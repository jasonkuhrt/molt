{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "composite": true
  },
  "include": ["./src"],
  // Prevent auto-import from build
  "exclude": ["./build"],
  "references": [
    {
      "path": "../types"
    }
  ]
}
